[project]
name = "ghrah-plugin"
version = "0.1.0a0"
description = "ghrah 插件基础设施（L2）——plugin spec 契约、发现/注册/协商纯函数与离线验证引擎"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "packaging>=23.0",
]
authors = [
    {name = "chenxya", email = "chenxya@ghrah.org"},
]
license = "Apache-2.0"
license-files = ["LICENSES/*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ghrah"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.8",
    "mypy>=1.9",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
strict = true

[tool.hatch.build.targets.sdist]
exclude = [
    "/.python-version",
    "/main.py",
    "/.kilo",
    "/plans",
    "**/__pycache__",
    "**/.mypy_cache",
    "**/.pytest_cache",
    "**/.ruff_cache",
]
